name: arcgis_cloning

channels:
  - esri
  - conda-forge

dependencies:
  - black>=21
  - pip
  - pytest
  - pip:
    - bump2version
    - mkdocs
    - mkdocs-glightbox
    - mkdocs-include-markdown-plugin
    - mkdocs-jupyter
    - mkdocs-material
    - mkdocstrings[python]>=0.18
